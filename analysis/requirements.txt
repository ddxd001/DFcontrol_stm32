# Python 3.11 win32: pin matplotlib to a version that ships prebuilt wheels.
# (Newer releases may fall back to source build and require Visual Studio.)
#
# Install (PowerShell, if pip fails with proxy error on Windows):
#   $env:NO_PROXY='*'; $env:no_proxy='*'
#   python -m pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
#
# Or:  .\setup_env.ps1

matplotlib==3.7.5
